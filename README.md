# StegaVault - Image Steganography Tool

Desktop application to hide/extract text inside images using LSB steganography.

## Features
- Hide text inside images (PNG/JPEG)
- Extract hidden text
- Modern PyQt5 GUI
- Save encoded image as PNG to avoid lossy compression

## Install
```bash
pip install -r requirements.txt
